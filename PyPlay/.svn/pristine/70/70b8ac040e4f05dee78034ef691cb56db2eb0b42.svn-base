#include "renderer.h"

const Vector3f DrawCallInfo::default_translation = Vector3f( 0.0f, 0.0f, 0.0f );
const Vector3f DrawCallInfo::default_rotation = Vector3f( 0.0f, 0.0f, 0.0f );
const Color DrawCallInfo::default_color = Color( Vector3ub(255, 255, 255) );

void export_renderer()
{
	/*
	class_<Renderer, boost::noncopyable>( "renderer", no_init );
	register_ptr_to_python< shared_ptr<Renderer> >();
	*/
}

void export_font()
{
	/*
	class_<Font, boost::noncopyable>( "font", no_init );
	register_ptr_to_python< shared_ptr<Font> >();
	*/
}